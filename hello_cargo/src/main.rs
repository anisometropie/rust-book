fn main() {
    println!("Hello, world!");
}

// cargo build
// cargo run – build and run
// cargo check  – much faster, if ào need to compile several times just to check 

// cargo build --release
// slow build but faster code
